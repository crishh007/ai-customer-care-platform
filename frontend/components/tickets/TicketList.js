// ASSIGNED TO: FE-4
// Props: tickets (array of ticket objects)
// - Render table with columns: ID, Subject, Status, Priority, Created At
// - Color-coded status badges
// - Click row to view ticket detail

export default function TicketList({ tickets = [] }) {
  return (
    <div>
      {/* TODO: Render ticket table */}
    </div>
  )
}
