// ASSIGNED TO: FE-3
// Props: data (array of { date, happy, frustrated, angry, neutral })
// - Use recharts BarChart or LineChart
// - Show emotion distribution over time
// Import: import { BarChart, Bar, XAxis, YAxis, Tooltip } from 'recharts'

export default function SentimentChart({ data = [] }) {
  return (
    <div>
      {/* TODO: Render recharts chart */}
    </div>
  )
}
