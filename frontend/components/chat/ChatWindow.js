// ASSIGNED TO: FE-2
// Props: messages (array), isLoading (bool)
// - Render message bubbles (user vs AI styling)
// - Auto-scroll to latest message
// - Show timestamp per message
// - Show SentimentBadge on AI messages

export default function ChatWindow({ messages = [], isLoading = false }) {
  return (
    <div>
      {/* TODO: Map through messages and render bubbles */}
    </div>
  )
}
