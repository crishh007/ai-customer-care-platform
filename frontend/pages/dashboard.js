// ASSIGNED TO: FE-3
// Build Analytics Dashboard:
// - Protected route (check JWT, redirect to /login if missing)
// - Fetch from GET /api/analytics/dashboard
// - Show KPI cards: Active Conversations, CSAT, Ticket count, Escalation rate
// - Sentiment heatmap chart (recharts)
// - AI confidence score trend

export default function Dashboard() {
  return (
    <div>
      <h1>Dashboard</h1>
      <p>TODO: Build analytics dashboard</p>
    </div>
  )
}
