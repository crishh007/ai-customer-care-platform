// ASSIGNED TO: FE-4
// Props: onSubmit (function)
// - Fields: Subject (text), Description (textarea), Priority (dropdown: Low/Medium/High)
// - Submit → POST /api/tickets/create
// - Show success / error feedback

export default function TicketForm({ onSubmit }) {
  return (
    <div>
      {/* TODO: Build form fields and submit handler */}
    </div>
  )
}
