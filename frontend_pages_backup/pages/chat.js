// ASSIGNED TO: FE-2 (UI) + FE-4 (Socket.IO real-time)
// Build Chat Interface:
// - Real-time messaging with Socket.IO
// - Send message → POST /api/chat/query
// - Load history → GET /api/chat/history
// - Display sentiment badge per message
// - Show AI confidence score
// - Typing indicator

export default function Chat() {
  return (
    <div>
      <h1>Chat</h1>
      <p>TODO: Build chat interface</p>
    </div>
  )
}
